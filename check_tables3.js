import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://rpaihoyotvinucbelaew.supabase.co', ''); async function check() { const { data, error } = await supabase.rpc('exec_sql', { query: "SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name LIKE 'cat_%';" }); console.log('TABLES:', data, 'ERROR:', error); } check();
